template: page
title: Home
summary: The scaffold homepage is a YAML file rendered through templates/index.html.
hero:
  title: Example Site
  subtitle: This page comes from content/index.yml and uses links generated from site_hooks.py.
docs_links: python::blogroll_links
hook_sentence: "First generated link: {{ docs_links[0].label }}"
feature_cards:
  - title: Config first
    text: colophon.yml points to content, templates, static assets, output, deploy config, and trusted Python modules.
    href: /features/
  - title: Content driven
    text: YAML pages, Markdown static pages, and Markdown posts all share the same template context.
    href: /about/
  - title: Template context
    text: Templates receive site, page, post, collections, pages, assets, and the image() helper.
    href: /template-variables/
  - title: Optional deploy
    text: Deploy can be checked with a dry run and environment-backed secrets.
    href: /deploy/
posts_section:
  heading: Latest scaffold posts
sidebar:
    cards:
    - type: note
      title: Hook output
      text: The sentence above is rendered from blogroll links returned by site_hooks.py.
    - type: note
      title: Generated output
      text: Build output appears in _site/. Static files copy directly, while content images can create generated derivatives.
